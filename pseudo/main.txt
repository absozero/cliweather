import requests
import click
import json

@click.command(weather, args=LOCATION)

class(API_URL):
    weather = requests.get(API_URL)

    json_api = weather.json()

    with open json_api:
        query "LOCATION"

        return weatherdata
    